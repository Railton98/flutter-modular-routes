import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('PaymentSuccessPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PaymentSuccessPage(title: 'PaymentSuccess')));
    //  final titleFinder = find.text('PaymentSuccess');
    //  expect(titleFinder, findsOneWidget);
  });
}
