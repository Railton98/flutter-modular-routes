import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('PaymentDataPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PaymentDataPage(title: 'PaymentData')));
    //  final titleFinder = find.text('PaymentData');
    //  expect(titleFinder, findsOneWidget);
  });
}
