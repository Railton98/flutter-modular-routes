import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('AddresDataPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(AddresDataPage(title: 'AddresData')));
    //  final titleFinder = find.text('AddresData');
    //  expect(titleFinder, findsOneWidget);
  });
}
