import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:modular_navigation_routes/app/modules/purchase/purchase_module.dart';

void main() {
  initModule(PurchaseModule());
  // PurchaseController purchase;
  //
  setUp(() {
    //     purchase = PurchaseModule.to.get<PurchaseController>();
  });

  group('PurchaseController Test', () {
    //   test("First Test", () {
    //     expect(purchase, isInstanceOf<PurchaseController>());
    //   });

    //   test("Set Value", () {
    //     expect(purchase.value, equals(0));
    //     purchase.increment();
    //     expect(purchase.value, equals(1));
    //   });
  });
}
