import 'package:flutter_test/flutter_test.dart';

main() {
  testWidgets('PurchasePage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PurchasePage(title: 'Purchase')));
    //  final titleFinder = find.text('Purchase');
    //  expect(titleFinder, findsOneWidget);
  });
}
